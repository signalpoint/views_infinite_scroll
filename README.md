# views_infinite_scroll
The Views Infinite Scroll module for DrupalGap.

#settings.js
```
drupalgap.settings.views_infinite_scroll = {
  pages_allowed: 2, // # of views results pages allowed in the DOM (per view)
};
```

